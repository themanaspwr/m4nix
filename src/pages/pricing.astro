---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Pricing — M4N!X">
  <section class="container" style="padding-top:3rem; padding-bottom:3rem;">
    <h1 style="font-size:2.5rem; margin-bottom:1.5rem;">Pricing</h1>
    <p style="color:var(--muted); max-width:560px; line-height:1.6;">
      Simple and transparent pricing. Pay only for what you need, and let AI build your digital presence.
    </p>

    <div class="pricing-grid">
      
      <!-- Starter -->
      <div class="pricing-card">
        <h3>Starter</h3>
        <p class="price">$0 <span>/ project</span></p>
        <ul>
          <li>1 AI-Generated Website</li>
          <li>Basic customizations</li>
          <li>Email support</li>
        </ul>
        <button class="pricing-btn" onclick="window.Clerk.openSignUp()">Get Started</button>
      </div>

      <!-- Pro -->
      <div class="pricing-card featured">
        <h3>Pro</h3>
        <p class="price">$29 <span>/ project</span></p>
        <ul>
          <li>Everything in Starter</li>
          <li>Advanced design edits</li>
          <li>AI automation flows</li>
          <li>Branding kit (logo + colors)</li>
          <li>Priority support</li>
        </ul>
        <button class="pricing-btn" onclick="window.Clerk.openSignUp()">Choose Pro</button>
      </div>

      <!-- Ultimate -->
      <div class="pricing-card">
        <h3>Ultimate</h3>
        <p class="price">$79 <span>/ project</span></p>
        <ul>
          <li>Everything in Pro</li>
          <li>Unlimited revisions</li>
          <li>Content creation package</li>
          <li>1-on-1 support</li>
        </ul>
        <button class="pricing-btn" onclick="window.Clerk.openSignUp()">Go Ultimate</button>
      </div>

    </div>
  </section>

  <style>
    .pricing-grid {
      margin-top:2.5rem;
      display:grid;
      gap:1.5rem;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    }

    .pricing-card {
      background: var(--card);
      border:1px solid rgba(0,0,0,0.06);
      padding:1.5rem;
      border-radius:16px;
      transition: transform .2s ease, box-shadow .2s ease;
    }

    .pricing-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 14px 32px rgba(11,18,32,0.12);
    }

    .pricing-card.featured {
      border:1px solid var(--accent);
      box-shadow: 0 14px 32px rgba(11,18,32,0.14);
    }

    .pricing-card h3 {
      margin:0 0 0.5rem 0;
      font-family: 'Comfortaa', sans-serif;
      font-size:1.4rem;
    }

    .price {
      font-size:2rem;
      font-weight:600;
      margin:0.5rem 0 1rem 0;
    }

    .price span {
      font-size:1rem;
      color:var(--muted);
    }

    .pricing-card ul {
      list-style:none;
      padding:0;
      margin:0 0 1.2rem 0;
      color:var(--muted);
      line-height:1.6;
    }

    .pricing-btn {
      width:100%;
      padding:0.65rem 1rem;
      font-size:1rem;
      border-radius:8px;
      border:none;
      background:var(--text);
      color:white;
      cursor:pointer;
      transition: all .2s;
    }

    .pricing-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 22px rgba(11,18,32,0.12);
    }
  </style>

</Layout>
